{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "buildCommand": "npm ci && npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/(assets|js|css)/(.*)", "dest": "/$1/$2" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
