╔══════════════════════════════════════════════════════════════════════════════╗
║                 APPROVAL-AWARE FINANCIAL REPORTS                             ║
║                        VISUAL IMPLEMENTATION GUIDE                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ CURRENT STATUS: ✅ READY TO DEPLOY                                           │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ SYSTEM ARCHITECTURE (CORRECTED)                                              │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │                    TRANSACTIONS TABLE                       │
    ├─────────────────────────────────────────────────────────────┤
    │ id                  │ UUID                                  │
    │ entry_date          │ DATE                                  │
    │ is_posted           │ BOOLEAN (false = draft)               │
    │ approval_status     │ TEXT (NULL/'draft'/'submitted'/       │
    │                     │       'approved'/'rejected')          │
    └─────────────────────────────────────────────────────────────┘
                                    │
                                    │ 1:N
                                    ▼
    ┌─────────────────────────────────────────────────────────────┐
    │                 TRANSACTION_LINES TABLE                     │
    ├─────────────────────────────────────────────────────────────┤
    │ id                  │ UUID                                  │
    │ transaction_id      │ UUID (FK)                             │
    │ account_id          │ UUID (FK)                             │
    │ debit_amount        │ NUMERIC                               │
    │ credit_amount       │ NUMERIC                               │
    └─────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ APPROVAL STATUS VALUES                                                       │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────┬──────────────┬────────────────────────────────────────┐
    │ Value       │ Arabic       │ Meaning                                │
    ├─────────────┼──────────────┼────────────────────────────────────────┤
    │ NULL        │ مسودة        │ Draft (not submitted)                  │
    │ 'draft'     │ مسودة        │ Draft (explicitly set)                 │
    │ 'submitted' │ مقدمة        │ Submitted (awaiting approval)          │
    │ 'approved'  │ معتمدة       │ Approved (ready to post)               │
    │ 'rejected'  │ مرفوضة       │ Rejected (needs revision)              │
    └─────────────┴──────────────┴────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ DATA FLOW                                                                    │
└──────────────────────────────────────────────────────────────────────────────┘

    USER SELECTS FILTER
           │
           ▼
    ┌─────────────────────────────────────────┐
    │  Trial Balance UI Component             │
    │  - TrialBalanceOriginal.tsx             │
    │  - TrialBalanceAllLevels.tsx            │
    └─────────────────────────────────────────┘
           │
           │ approvalStatus: 'draft' | 'submitted' | 'approved' | 'rejected' | null
           ▼
    ┌─────────────────────────────────────────┐
    │  Unified Financial Query Service        │
    │  - unified-financial-query.ts           │
    │  - fetchGLSummary(filters)              │
    └─────────────────────────────────────────┘
           │
           │ p_approval_status parameter
           ▼
    ┌─────────────────────────────────────────┐
    │  Database Function (PostgreSQL)         │
    │  - get_gl_account_summary_filtered()    │
    │  - Filters transactions by status       │
    └─────────────────────────────────────────┘
           │
           │ GL Summary Data
           ▼
    ┌─────────────────────────────────────────┐
    │  Trial Balance Report Display           │
    │  - Shows filtered accounts              │
    │  - Shows period totals (905M)           │
    │  - Shows closing balances (204M)        │
    └─────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ UI FILTER APPEARANCE                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────────────────┐
    │ Trial Balance Report                                               │
    ├────────────────────────────────────────────────────────────────────┤
    │                                                                    │
    │  Organization: [ABC Company]  Project: [All Projects]             │
    │                                                                    │
    │  From: [2025-01-01]  To: [2025-12-31]                             │
    │                                                                    │
    │  Approval Status: [▼ All Status        ]  ← NEW FILTER            │
    │                    ├─ All Status                                  │
    │                    ├─ Draft (مسودة)                               │
    │                    ├─ Submitted (مقدمة)                           │
    │                    ├─ Approved (معتمدة)                           │
    │                    └─ Rejected (مرفوضة)                           │
    │                                                                    │
    │  [🗂️ Show All] [Excel] [CSV] [PDF] [Print] [Refresh]             │
    │                                                                    │
    └────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ CURRENT DATA STATE                                                           │
└──────────────────────────────────────────────────────────────────────────────┘

    Total Transactions: 2,161
    ├─ Draft (NULL/draft):     2,161 (100%)
    ├─ Submitted:              0 (0%)
    ├─ Approved:               0 (0%)
    └─ Rejected:               0 (0%)

    Period Totals (Transaction Volume):
    ├─ Period Debits:          905,925,674.84
    └─ Period Credits:         905,925,674.84

    Closing Balances (Net Position):
    ├─ Closing Debits:         204,937,398.11
    └─ Closing Credits:        204,937,398.11

    ✅ Trial Balance is BALANCED (difference: 0.0002)

┌──────────────────────────────────────────────────────────────────────────────┐
│ DEPLOYMENT CHECKLIST                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

    ☐ Step 1: Run SQL file in Supabase
       File: sql/create_approval_aware_gl_summary_FIXED.sql
       
    ☐ Step 2: Verify test results
       Expected: 5 test queries + 1 diagnostic query
       
    ☐ Step 3: Test UI filter at /reports/trial-balance
       - Select "All Status" → Should show 2,161 transactions
       - Select "Draft" → Should show 2,161 transactions
       - Select "Submitted" → Should show 0 transactions
       - Select "Approved" → Should show 0 transactions
       - Select "Rejected" → Should show 0 transactions
       
    ☐ Step 4: Test UI filter at /reports/trial-balance-all-levels
       - Repeat same tests as Step 3
       
    ☐ Step 5: Test date filtering
       - Change date range
       - Verify report updates correctly
       - Verify approval filter still works
       
    ☐ Step 6: Test export functions
       - Export to Excel → Check approval status in metadata
       - Export to CSV → Check approval status in metadata
       - Export to PDF → Check approval status in header
       - Print report → Check approval status in header

┌──────────────────────────────────────────────────────────────────────────────┐
│ TESTING APPROVAL WORKFLOW (OPTIONAL)                                         │
└──────────────────────────────────────────────────────────────────────────────┘

    To test the approval filter with real data:

    1. Submit 10 transactions:
       ┌────────────────────────────────────────────────────────────────┐
       │ UPDATE transactions                                            │
       │ SET approval_status = 'submitted'                              │
       │ WHERE id IN (SELECT id FROM transactions LIMIT 10);            │
       └────────────────────────────────────────────────────────────────┘

    2. Approve 5 of them:
       ┌────────────────────────────────────────────────────────────────┐
       │ UPDATE transactions                                            │
       │ SET approval_status = 'approved'                               │
       │ WHERE id IN (                                                  │
       │   SELECT id FROM transactions                                  │
       │   WHERE approval_status = 'submitted'                          │
       │   LIMIT 5                                                      │
       │ );                                                             │
       └────────────────────────────────────────────────────────────────┘

    3. Test filter in UI:
       ├─ All Status:   2,161 transactions
       ├─ Draft:        2,151 transactions
       ├─ Submitted:    5 transactions
       ├─ Approved:     5 transactions
       └─ Rejected:     0 transactions

┌──────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

    ❌ Error: "relation 'public.transaction_approvals' does not exist"
       → You're using the OLD SQL file
       → Use: sql/create_approval_aware_gl_summary_FIXED.sql

    ❌ Error: "column tl.expenses_category_id does not exist"
       → You're using the OLD SQL file
       → Use: sql/create_approval_aware_gl_summary_FIXED.sql

    ❌ Error: "syntax error at or near '$'"
       → You're using the OLD SQL file
       → Use: sql/create_approval_aware_gl_summary_FIXED.sql

    ❌ Filter doesn't appear in UI
       → Clear browser cache
       → Hard refresh (Ctrl+Shift+R)
       → Check TypeScript compilation

    ❌ Filter doesn't change data
       → Check browser console for errors
       → Verify database function was deployed
       → Check approval_status values in database

┌──────────────────────────────────────────────────────────────────────────────┐
│ FILES MODIFIED                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

    ✅ sql/create_approval_aware_gl_summary_FIXED.sql
       - Database function with approval status support
       - Includes test queries
       - Ready to deploy

    ✅ src/services/reports/unified-financial-query.ts
       - Updated UnifiedFilters interface
       - Added approval status parameter
       - Passes to database function

    ✅ src/pages/Reports/TrialBalanceOriginal.tsx
       - Added approval status state
       - Added approval status dropdown
       - Auto-reloads on filter change

    ✅ src/pages/Reports/TrialBalanceAllLevels.tsx
       - Added approval status state
       - Added approval status dropdown
       - Auto-reloads on filter change

┌──────────────────────────────────────────────────────────────────────────────┐
│ NEXT STEPS                                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

    1. Deploy database function:
       → Open Supabase SQL Editor
       → Run: sql/create_approval_aware_gl_summary_FIXED.sql
       → Verify test results

    2. Test in UI:
       → Navigate to /reports/trial-balance
       → Test approval status filter
       → Verify data updates correctly

    3. (Optional) Post all transactions:
       → Run: sql/fix_trial_balance_post_all_transactions.sql
       → This sets is_posted = true for all transactions

    4. (Optional) Test approval workflow:
       → Submit some transactions
       → Approve some transactions
       → Test filter with real approval data

╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║  ✅ ALL CODE CHANGES COMPLETE                                                ║
║  ✅ DATABASE FUNCTION READY TO DEPLOY                                        ║
║  ✅ UI COMPONENTS UPDATED                                                    ║
║  ✅ APPROVAL STATUS FILTER INTEGRATED                                        ║
║                                                                              ║
║  NEXT ACTION: Deploy SQL file and test in UI                                ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
